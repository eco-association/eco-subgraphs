{
    "name": "Policy",
    "license": "UNLICENSED",
    "scripts": {
        "graph-node": "node scripts/runGraphNode.js",
        "network": "node scripts/setupNetwork.js :",
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ paged1/Policy",
        "create-local": "graph create --node http://localhost:8020/ paged1/Policy",
        "remove-local": "graph remove --node http://localhost:8020/ paged1/Policy",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 paged1/Policy"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.29.1",
        "@graphprotocol/graph-ts": "0.26.0",
        "yaml": "^2.1.1"
    }
}
